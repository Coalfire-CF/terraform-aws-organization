name: Org Release

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: read

on:
  push:
    branches:
      - main

jobs:
  create-release:
    uses: Coalfire-CF/Actions/.github/workflows/org-release.yml@v0.3.2
    secrets: inherit
    
